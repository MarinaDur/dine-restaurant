import { styled } from "styled-components";

const LinesPattern = styled.div`
  @media (min-width: 1024px) {
    position: absolute;
    width: 160px;
    height: 80px;
    background: url("./patterns/pattern-lines.svg") no-repeat;
  }
`;

export default LinesPattern;

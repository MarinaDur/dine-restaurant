import { css } from "styled-components";

const error = css`
  color: var(--cl-red);
  font-size: 1rem;
  font-weight: 500;
  line-height: normal;
  letter-spacing: -0.125px;
`;

export default error;
